<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="UNDAI.VIEWS.MASTER.MainPageMasterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:UNDAI.VIEWS.MASTER"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">


    <Viewbox
        x:Name="viewBox1"
        Margin="10"
        HorizontalAlignment="Center"
        VerticalAlignment="Top"
        Stretch="Uniform"
        StretchDirection="Both"
        UseLayoutRounding="True">

        <StackPanel>
            <TextBlock Text="MainPage" />
            <Button Command="{x:Bind ViewModel.SubstationMasterCommand}" />
            <Button Command="{x:Bind ViewModel.SystemSettingMasterCommand}" />

        </StackPanel>

    </Viewbox>
</Page>
